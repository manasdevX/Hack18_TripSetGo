{
  "name": "tripsetgo",
  "version": "1.0.0",
  "description": "TripSetGo — AI-powered travel planner (MERN stack)",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n backend,frontend -c cyan,magenta \"npm run dev -s --prefix backend\" \"npm run dev -s --prefix frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
